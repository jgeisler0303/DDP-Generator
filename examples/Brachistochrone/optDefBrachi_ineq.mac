x: [y];
u: [dy];

f[y]: y + dy*dx;

assume(dx>0);
assume(y<0);
assume(dy<0);

L: factor(expand(integrate(sqrt((1+dy^2)/(2*g*abs(y+x_*dy))), x_, 0, dx)));
F: 0;

hfi[1]: y-yf;
/*h[1]: dy; */
